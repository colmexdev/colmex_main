<%= form_for(@tipo_curso) do |f| %>
  <% if @tipo_curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_curso.errors.count, "error") %> prohibited this tipo_curso from being saved:</h2>

      <ul>
      <% @tipo_curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br>
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
