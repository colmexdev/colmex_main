<% if admin.signed_in && (current_admin.admin == "Administrador" || current_admin.admin == "Editor") %>
<p id="notice"><%= notice %></p>

<h1>Listing Catedras</h1>

<table>
  <thead>
    <tr>
      <th>Titulo</th>
      <th>Descripcion</th>
      <th>Liga</th>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @catedras.each do |catedra| %>
      <tr>
        <td><%= catedra.titulo %></td>
        <td><%= catedra.descripcion %></td>
        <td><%= catedra.liga %></td>
        <td><%= catedra.tipo %></td>
        <td><%= link_to 'Show', catedra %></td>
        <td><%= link_to 'Edit', edit_catedra_path(catedra) %></td>
        <td><%= link_to 'Destroy', catedra, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Catedra', new_catedra_path %>
<%= link_to "Regresar", panel_path %>
<% end %>
