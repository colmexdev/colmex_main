    <p>&nbsp;</p>
    <ul class=" uk-list">
    <% estatutos.each do |e| %>
    <li><img src=<%= image_url("documento.png") %> alt="documento" class="documento uk-vertical-align-middle"><a href=<%= documento.liga.to_s == "" ? documento.archivo.url : documento.liga %> target="_blank" class="documento"><%= e.nombre %></a></li>
    <% end %>
    </ul>
    <p>&nbsp;</p>
